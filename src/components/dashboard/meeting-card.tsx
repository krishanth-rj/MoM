export function MeetingCard(_props: {
  meeting: {
    meeting_id: string;
    title: string;
    status: string | null;
    date: string | null;
  };
}) {
  return null; // Not used anymore — dashboard renders inline
}
