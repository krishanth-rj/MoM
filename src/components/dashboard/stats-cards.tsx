export function StatsCards({
  meetingsCount,
  thisMonthCount,
  completedCount,
}: {
  meetingsCount: number;
  thisMonthCount: number;
  completedCount: number;
}) {
  return null; // Not used anymore — dashboard has marquee stats
}
